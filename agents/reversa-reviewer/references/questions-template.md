# Template — _reversa_sdd/questions.md

This file is generated by the Reviewer and filled by the user.

---

## How the Reviewer Creates This File

For each 🔴 gap that only the user can resolve, create an entry:

```markdown
## Question [N]

**Context:** [Where in the system this question arose — module, function, line]
**Affected spec:** [`_reversa_sdd/<unit>/{requirements|design|tasks}.md`]
**Question:** [Direct question, unambiguous]
**Impact:** [What changes in the spec depending on the answer]

**Answer:** <!-- fill in here -->
```

---

## How the User Answers

The user must:
1. Fill in the `**Answer:**` field for each question
2. Notify the Reviewer when done (type `reversa` or "I answered the questions")

---

## How the Reviewer Processes Responses

After receiving user notification:
1. Read the complete file
2. For each question with a filled answer:
   - Update the corresponding spec
   - Reclassify the item per the answer (🔴→🟢 or 🔴→🟡)
   - Mark the question as `✅ Answered` above the Answer field
3. For unanswered questions: keep as 🔴 in the final report
4. Update `confidence-report.md` with the new percentages

---

## Example of Generated File

```markdown
# Questions for Validation — [Project Name]

> Generated by the Reviewer on [date]
> Answer each question and let me know when you're done.

---

## Question 1

**Context:** `auth` module — `validateSession()` function in `src/auth/session.ts:47`
**Affected spec:** [`_reversa_sdd/auth/requirements.md`]
**Question:** Does the session expire due to inactivity or only by absolute time? The code uses `lastActivity` but there is no visible expiration logic.
**Impact:** If it is by inactivity, the spec needs to include the maximum idle time and the behavior when renewing the session.

**Answer:** <!-- fill in here -->

---

## Question 2

**Context:** `orders` module — constant `MAX_ITEMS_PER_ORDER = 50` in `src/orders/constants.ts:12`
**Affected spec:** [`_reversa_sdd/orders/requirements.md`]
**Question:** Is the 50 items per order limit a business rule or a temporary technical limit? Are there plans to increase it?
**Impact:** If it is a business rule, it needs to be listed as 🟢 in the restrictions. If it is technical, it should be 🟡 with a note for future review.

**Answer:** <!-- fill in here -->
```
